# Requirements

## User Stories

- As a developer, I want to add unit tests to the notification service to ensure code reliability and catch regressions.

## Acceptance Criteria

- Unit tests are added for key functions in the notification service.
- Tests can be run using `go test ./...` and pass without errors.
- Test coverage is basic, focusing on the main handler and sender logic.